{$DOMAIN} {
    encode zstd gzip

    @noauth not header X-API-Key {$API_KEY}
    respond @noauth "Unauthorized" 401

    reverse_proxy ollama:11434 {
        transport http {
            keepalive 30s
        }
    }
}